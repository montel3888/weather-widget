<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Node JS APP</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  
  <div class="app">
    <h1>Weather NodeJs Application</h1>
    <div class="app__header">
      <% if(weather) {%>
        <h1 class="success"><%=weather %> °C</h1>
      <% } %>
      <% if(error) {%>
        <p class="error"><%=error %></p>
      <% } %>
    </div>
    <form action="/" method="POST" class="app__content">
      <div class="input">
        <label for="city">City</label>
        <input type="text" id="city" placeholder="City name" name="city">
      </div>
      <button type="submit" class="btn">Show weather</button>
    </form>
  </div>
</body>
</html>